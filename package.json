{
  "name": "tour",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.17"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --hot src/index.ts",
    "build": "bunx drizzle-kit generate:sqlite --schema src/db/schema.ts && bun build --compile src/index.ts --outfile coconut_travel"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "@types/bun": "^1.1.1",
    "drizzle-orm": "^0.30.10",
    "fs": "^0.0.1-security"
  },
  "trustedDependencies": [
    "es5-ext",
    "esbuild"
  ]
}